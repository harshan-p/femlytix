# .streamlit/secrets.toml
# ══════════════════════════════════════════════════════════════
# FEMLYTIX — SMTP Email Configuration
# Copy this file to .streamlit/secrets.toml and fill in values.
# NEVER commit secrets.toml to git — add it to .gitignore
# ══════════════════════════════════════════════════════════════

[smtp]

# ── Option A: Gmail (most common) ─────────────────────────────
# Step 1: Enable 2-Factor Authentication on your Google account
#         → myaccount.google.com → Security → 2-Step Verification
# Step 2: Create an App Password
#         → myaccount.google.com → Security → App Passwords
#         → Select "Mail" + "Other (Custom name)" → Copy 16-char password
host       = "smtp.gmail.com"
port       = 587
username   = "harshanp262@gmail.com"
password   = "quqj wnkn xkhn vddu"   # 16-char App Password (spaces OK)
from_email = "harshanp262@gmail.com"
from_name  = "FEMLYTIX"